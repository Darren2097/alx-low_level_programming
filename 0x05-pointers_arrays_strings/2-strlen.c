#include "main.h"
#include <string.h>

/**
 * _strlen - Returns the length of a string
 * @s: string variable
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (s);
}
