#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: string variable
 * @s2: string variable
 *
 * Return: returns 0 if the same, less than 0 if s1<s2 and more than 0 if s2>s1
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
