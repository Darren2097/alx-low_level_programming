#include "main.h"

/**
 * *infinite_add - adds two numbers
 * @n1: first number
 * @n2: second number
 * @r: result of the numbers added
 * @size_r: buffer size
 *
 * Return: returns variable r and 0 if the result can't be stored in r
 */

char *infinte_add(char *n1, char *n2, char *r, int size_r)
{

	r = n1 + n2;
}
