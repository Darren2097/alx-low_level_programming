#include "main.h"
#include <ctype.h>

/**
 * _islower - Checks for lowercase character
 *
 * Return: Always 0 (success)
 */

int _islower(int c)
{
	inp = getchar();
	int c = islower(inp);

	if (c > 0)
	{
		return (1);
	else
		return (0);
	}
}
